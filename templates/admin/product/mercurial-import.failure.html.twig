<turbo-stream action="replace" target="mercurial_import_form_container">
    <template>
        {% for error in errors %}
            <p class="alert alert-danger" role="alert">{{ error.message }}</p>
        {% endfor %}
        <a href="{{ path('admin_product_mercurial_import') }}" class="btn btn-secondary">Go back</a>
    </template>
</turbo-stream>
